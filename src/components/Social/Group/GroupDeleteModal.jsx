export default function GroupDeleteModal({ groupId }) {
  return (
    <div style={styles.container}>
    </div>
  )
}

const styles = {
  container: {
    width: 400,
    height: 600,

    display: 'flex',
    flexDirection: 'column',
  },
}