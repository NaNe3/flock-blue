export default function AddGroupByCodeModal() {
  return (
    <div style={styles.container}>
    </div>
  )
}

const styles = {
  container: {
    width: 800,
    height: 600,

    display: 'flex',
    flexDirection: 'column',
  },
}